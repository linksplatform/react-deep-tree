name: Folders-root
 
# When to launch. During which event
on: push

jobs:
  gh-pages:
    runs-on: ubuntu-latest 
    
    steps:
      - name: clone
        uses: actions/checkout@v1     
         
         
      - name: open-sh 
        run: |    
             cd example  
             chmod +x publish.sh
             ./publish.sh 
             ls -la  
      
